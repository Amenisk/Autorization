@using Autorization.Data

<div align="right">
    <ul>
        @foreach(var user in Users)
        {
            <li>"@user.Name @user.Surname"</li>
        }
    </ul>
</div>

@code {
    [Parameter]
    public List<User> Users { get; set; } = new List<User>();

    private void GetCard(string name, string surname)
    {

    }
}
